services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - BASE_URL=http://localhost:8000
      - DATABASE_URL=sqlite:///./data/encurtador.db
    volumes:
      - ./data:/app/data
